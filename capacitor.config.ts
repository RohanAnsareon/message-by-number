import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'message-by-number',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
